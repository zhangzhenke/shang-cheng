.search_input {
  /* 高 宽 */
  height: 90rpx;
  padding: 10rpx;
  background-color: var(--themeColor);
  /* 伸缩 居中 边框 */
}
.search_input navigator {
  height: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  background-color: #fff;
  border-radius: 15rpx;
  color: #666;
}
